"""exp-001 — MLX KV-compression × tool-calling.

Runner instances for the experiment documented under
``docs/experiments/exp-001-mlx-kv-compression-toolcalling/``. Each module here
has a ``main()`` entry point dispatched by the ``agentic-evals`` CLI:

    uv run agentic-evals exp-001 <runner>

where ``<runner>`` is the module name with underscores written as hyphens
(e.g. ``cliff-fine-sweep`` → :mod:`.cliff_fine_sweep`).
"""
